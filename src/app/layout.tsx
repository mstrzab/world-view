import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "WorldView - Geospatial Intelligence Dashboard",
  description: "Real-time 3D geospatial intelligence dashboard with satellite tracking, aviation monitoring, traffic analysis, and CCTV surveillance.",
  keywords: ["GIS", "CesiumJS", "Geospatial", "Satellite", "Aviation", "Traffic", "Intelligence", "Dashboard"],
  authors: [{ name: "WorldView Team" }],
  icons: {
    icon: "/logo.svg",
  },
  openGraph: {
    title: "WorldView Geospatial Dashboard",
    description: "Real-time 3D geospatial intelligence platform",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <head>
        <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
        <meta name="theme-color" content="#0a0a0f" />
        <meta name="apple-mobile-web-app-capable" content="yes" />
        <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
      </head>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
